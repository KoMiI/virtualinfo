import { createStore } from 'vuex'
import Auth from './modules/auth.js'

export default createStore({
  state: {
  },
  getters: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    Auth,
  }
})
